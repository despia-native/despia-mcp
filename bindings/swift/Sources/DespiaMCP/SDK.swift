// Re-export the exact-pinned official SDK so consumers that need its transports
// use the same protocol types as this package rather than resolving a second
// SDK version beside it.
@_exported import MCP
